silo:
  mailerRecipients:
    - '${mailerRecipients}'

docker:
  upstreamProjects:
    - 'base-image-builder-maven-docker'
  mailerRecipients:
    - '${mailerRecipients}'
  buildScript: 'deployment/bin/build'
  dockerFilePaths:
    - 'deployment/docker/Dockerfile'
  repository: ${rootArtifactId}-silo
