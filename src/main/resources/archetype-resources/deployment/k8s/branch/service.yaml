---
apiVersion: v1
kind: Service

metadata:
  name: ${projectName}
  labels:
    team: ${team}
    app: ${projectName}

spec:
  ports:
    - port: 80
      targetPort: 8080
      name: http
  selector:
    app: ${projectName}
